# Andy Hayes' Dev Task:

A very special project for those with C.I.A. clearance only.

To run the project just go here: https://andy-hayes.com/web/andy-hayes-devtask/

To build and run this project from source just checkout this branch and follow the instructions below:

### This project was forked from @hayesmaker's create-react-app based boilerplate:
### react-express-template
Template for React Client and Node Express backend

Installs Your React and Babel powered `client` files, and serves express
app from another port.
Client uses concurrently to proxy requests to the express app to avoid
CORS errors.

### Prerequisites
- NodeJSd

### Installation

```
npm i && cd client && npm i
```

### Run
`(From project root)`
```
npm start

```
- http://localhost:3000 in browser
- Express app will be served at localhost:3001

### Test
- A quick test of the url above in any browser should show a the CIA dev task project

